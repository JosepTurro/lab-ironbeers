{{#each beers}}

<div class="birrot">
    <div class="birrotImg">
        <img src="{{this.image_url}}" alt="beer : me ccagon dios">
    </div>

    <div>
        <h1>{{this.name}}</h1>
        <h3>{{this.tagline}}</h3>
        <p>{{this.description}}</p>
    </div>
</div>


{{/each}}